<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="18dp"
        android:paddingBottom="12dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/appointments_title"
            android:textColor="@color/text_dark"
            android:textSize="24sp"
            android:textStyle="bold" />

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_calendar"
            android:background="@drawable/circle_purple_light"
            android:padding="8dp"
            android:contentDescription="@string/appointments_icon_desc" />
    </LinearLayout>

    <TextView
        android:id="@+id/tabUpcoming"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tab_upcoming"
        android:textSize="15sp"
        android:textStyle="bold"
        android:paddingBottom="8dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/header"
        android:layout_marginStart="20dp"
        android:layout_marginTop="4dp" />

    <TextView
        android:id="@+id/tabActive"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tab_active"
        android:textSize="15sp"
        android:textStyle="bold"
        android:paddingBottom="8dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintStart_toEndOf="@id/tabUpcoming"
        app:layout_constraintTop_toBottomOf="@id/header"
        android:layout_marginStart="24dp"
        android:layout_marginTop="4dp" />

    <TextView
        android:id="@+id/tabCompleted"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tab_completed"
        android:textSize="15sp"
        android:paddingBottom="8dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintStart_toEndOf="@id/tabActive"
        app:layout_constraintTop_toBottomOf="@id/header"
        android:layout_marginStart="24dp"
        android:layout_marginTop="4dp" />

    <View
        android:id="@+id/indicator"
        android:layout_width="0dp"
        android:layout_height="2dp"
        android:background="@color/purple_primary"
        app:layout_constraintStart_toStartOf="@id/tabUpcoming"
        app:layout_constraintEnd_toEndOf="@id/tabUpcoming"
        app:layout_constraintTop_toBottomOf="@id/tabUpcoming" />

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#F0EFFF"
        app:layout_constraintTop_toBottomOf="@id/tabUpcoming"
        android:layout_marginTop="8dp" />

    <!-- Offline Indicator -->
    <TextView
        android:id="@+id/tvOfflineIndicator"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFE0E0"
        android:gravity="center"
        android:padding="4dp"
        android:text="@string/offline_mode_msg"
        android:textColor="#D32F2F"
        android:textSize="12sp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/divider" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAppointments"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:paddingHorizontal="20dp"
        android:paddingTop="12dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/tvOfflineIndicator"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Loading Spinner -->
    <ProgressBar
        android:id="@+id/pbLoading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:indeterminateTint="@color/purple_primary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/divider" />

</androidx.constraintlayout.widget.ConstraintLayout>